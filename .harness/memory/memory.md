# Memoria del Proyecto

## Proyecto

- code-style.md: Estilo de código del equipo
- branch-naming.md: Convención de nomenclatura de branches
- docs/ARCHITECTURE.md: Decisiones técnicas y modelo de datos
- docs/ROADMAP.md: Visión de versiones

## Reglas críticas

- **NUNCA hacer cambios sobre `main`.** Todo cambio se hace en una branch nueva creada desde `main` actualizado (primero `git pull origin main`). Si el AI detecta que está sobre `main`, debe frenar y crear la branch antes de tocar cualquier archivo.
- **Después de crear un PR, mover a `main` actualizada** (`git checkout main && git pull origin main`) para quedar posicionado para la próxima tarea. Solo posicionarse, sin tocar archivos.
- **Al crear PRs, siempre asignar al usuario** con `gh pr edit <num> --add-assignee slaveofthecode` (después de crear el PR con `gh pr create`).

## Usuario

- Prefiere que el AI aclare conceptos antes de implementar (MCP, permisos, proveedores AI).
- Decide por opciones simples y seguras: chat solo lectura, local-first (Ollama), sin features inventadas en docs.
- GitHub: `slaveofthecode`. Siempre asignarle los PRs creados por la AI.

## Sesión

- 2026-07-27: Creado harness inicial, definido stack
- 2026-07-29: Creado website público, definida convención de branches
- 2026-07-29: Creada v1 Visual Prototype con Ink + React. Estructura documental definida: README para humanos, AGENTS.md para AI, docs/ para documentación técnica, .harness/ para config AI. Migrado architecture.md de .harness a docs/.
- 2026-07-30: Tester ahora también valida calidad de código contra code-style.md. Eliminado CHANGELOG.md por duplicación con memory.md + README.md. Expandido code-style.md con reglas React (no useCallback sin beneficio, no non-null assertions en producción).
- 2026-08-03: Completadas v1 (persistencia SQLite con `bun:sqlite`) y v2 (ingreso de datos + CRUD completo). Documentado el harness en README e index.html (skills, commands, memory, conventions con quién/cuándo) y agregada sección "Run It Live" en el website.
- 2026-08-03: Reforzada la regla "nunca trabajar sobre main" (explicitar el checkout a una branch nueva antes de cualquier cambio) en AGENTS.md, branch-naming.md y memory.md, tras detectarse cambios aplicados directamente sobre `main`.
- 2026-08-05: Al crear un PR, el AI ahora vuelve a `main` actualizada (`git checkout main && git pull origin main`) para preparar la próxima tarea. Creado el agente `reviewer` que faltaba. Definido alcance de v5 (chat AI con Ollama local, tools solo lectura, MCP server al final) en ROADMAP.md.
- 2026-08-05: Registrados `developer`, `tester` y `reviewer` como subagentes reales en las 4 herramientas (opencode, Claude Code, GitHub Copilot, Cursor) con contexto aislado y permisos forzados, más los commands `test`/`pr` y el skill `code-review` por tool. `orchestrator` queda como rol del agente principal (jerarquía plana: los subagentes no lanzan subagentes). Las copias nativas se mantienen en sincronía manual desde `.harness/`.
- 2026-08-05: Resuelto el hallazgo anterior: el harness ya no es solo documentación — los agentes/commands/skills se registran nativamente por tool (opencode: `.opencode/` + `opencode.json`; Claude: `.claude/`; Copilot: `.github/`; Cursor: `.cursor/`). La AI principal sigue jugando el rol de orchestrator.
- 2026-08-05: Agregada convención de sincronización del harness en AGENTS.md: todo cambio de agent/skill/command se hace en `.harness/` y se replica en las 4 tools nativas, con entrada de sesión en memory.md y validación (checklist anti-drift).
- 2026-08-06: Completada v3 (Búsqueda y Filtros): `src/utils/filters.ts` con `filterItems`/`filterExpenses`/`searchItems`/`searchResults` puros y testeables (reutilizables en la tool `search_expenses` de v5), búsqueda global con `SearchPalette` en Dashboard, ItemDetail e ItemDetailCard, y tecla `t` para ciclar filtro por tipo en Dashboard. Filtros sobre datos en memoria, sin IA. Validado con tsc + 81 tests + smoke test de render.
- 2026-08-10: Unificado el estilo visual de las grillas (Dashboard, ItemDetail, ItemDetailCard): celdas de mes grises, header del mes actual blanco bold (resto blanco sin bold), gastos compartidos amarillos, primera cuota verde, y fila seleccionada solo en bold sin cambio de color. Rediseñada la búsqueda: `SearchPalette` reemplaza a `CommandPalette` (`/` abre input directo con resultados en vivo por gasto: item, tipo, persona, monto y fecha; Enter navega al item), `focusExpenseId` opcional en el screen `itemDetail` posiciona el cursor en la fila del gasto que matcheó (App ajusta el año antes de navegar). Footer actualizado: `/ Buscar`, eliminada la opción `t Tipo` (la tecla `t` sigue funcionando, ya no se muestra). Sincronizados ROADMAP.md e index.html. Validado con tsc + 93 tests.
- 2026-08-10: Definido v4 = solo gráficos en terminal (sin exportación CSV/JSON ni resumen mensual por ítem, sacados del alcance). Completada la pantalla `Charts` (tecla `g` en Dashboard, hint en footer): 4 gráficos con `1`-`4` para cambiar (gasto mensual con tu parte en amarillo, por tipo de ítem con %, top 5 ítems, distribución segmentada con leyenda), `←`/`→` cambia el año y `Esc` vuelve. `src/utils/charts.ts` con `computeCharts`/`scaleBlocksMin`/`distributeSegments` puros y testeables (mínimo 1 bloque para valores > 0) sobre `calcYearlySummaries`; nuevo screen `{ name: "charts" }` en `App.tsx`; barras adaptativas al ancho del terminal con la paleta de colores existente. Sincronizados ROADMAP.md e index.html. Validado con tsc + 112 tests + smoke test de render de los 4 gráficos.
- 2026-08-10: Iniciada v5 (AI Analysis) por sub-tareas, una branch por sub-task. Completado el paso 1: registry de tools de lectura en `src/ai/tools.ts` (`AiTool` con `name`/`description`/`parameters`/`execute`/`readonly`, `buildTools(ctx)` inyecta dependencias para testear sin DB, `createReadTools(db)` bindea SQLite, `getTool` por nombre). 5 tools de solo lectura reutilizando lo existente: `list_items`, `list_expenses` (filtros opcionales), `get_monthly_summary`, `get_yearly_summary`, `search_expenses` (searchResults de v3). Validan argumentos y tiran errores descriptivos. Este registry es el contrato único que usarán el agente, el chat y el MCP server. Marcadas v4 y "tools de consulta" como `[x]` en ROADMAP.md. Validado con tsc + 130 tests.
- 2026-08-13: Completado el paso 2 de v5: `src/ai/provider.ts` con la interfaz `LLMProvider` intercambiable (`chat(messages, tools)` con tool-calling + `checkHealth()`) y la implementación Ollama con `fetch` nativo a `OLLAMA_HOST` (default `http://localhost:11434`), modelo por `AI_MODEL` (default `llama3.2`). Helpers `normalizeHost`/`ollamaConfigFromEnv`, errores `OllamaConnectionError`/`OllamaApiError`, y health-check que reporta si Ollama está corriendo y si el modelo está descargado (`ollama pull <model>`). El formato de tools y mensajes sigue la OpenAPI de `/api/chat` (tools OpenAI-style, arguments string u object, round-trip de tool calls con `role: "tool"`). Tests con `fetch` mockeado e inyectado (env, payload de tools, round-trip, errores de red/API, health-check). Marcado "Módulo src/ai/ con interfaz de proveedor" como `[x]` en ROADMAP.md. Validado con tsc + 145 tests.
- 2026-08-13: Completado el paso 3 de v5: `src/ai/agent.ts` con el loop de tool-calling (`createAgent({ provider, tools, systemPrompt?, maxIterations? })` devuelve `Agent.ask(question)` → `{ answer, toolCallCount }`). System prompt de dominio en español (`DOMAIN_SYSTEM_PROMPT`): tipos de ítem, prorrateo de cuotas, ownership/`myShare`, montos ARS con formato es-AR y qué tool usar para cada pregunta. Los resultados de tools se serializan y vuelven al modelo con `role: "tool"`; errores de tools (argumentos inválidos, tool desconocida) se devuelven como JSON de error para que el modelo se recupere. Guard contra loops: máx `maxIterations` (default 8) por consulta; al superarse devuelve un mensaje de límite. Los errores de provider se propagan. Tests con `FakeProvider` (9 casos: respuesta directa, round-trip de tool calls, errores de tools, parallel calls, límite de iteraciones, custom prompt, propagación de errores). Actualizado el árbol de ARCHITECTURE.md. Validado con tsc + 154 tests.
- 2026-08-14: Completado el paso 4 de v5 (Chat AI en la TUI): nuevo screen `{ name: "chat" }` con el componente `src/app/components/Chat.tsx` (tecla `c` en Dashboard, hint en footer). Primera versión solo lectura, sin streaming: input libre en la parte inferior, Enter envía la pregunta, historial de mensajes (usuario/IA), error de conexión/mensaje en rojo, indicador "pensando…", y límite de 20 mensajes visibles. `App.tsx` construye el agente una sola vez (`useState(() => createAgent(...))`) sobre `createOllamaProvider()` + `createReadTools(db)`. Fix en `src/ai/provider.ts` descubierto con el smoke end-to-end contra Ollama real: en el round-trip de tool calls, `arguments` DEBE ir como objeto (`{ month: "2026-03" }`) y NO como string JSON (`'{"month":"2026-03"}'`) — Ollama responde 400 "Value looks like object, but can't find closing '}' symbol". Ajustado el test de round-trip en provider.test.ts. Sincronizados ROADMAP.md (chat marcado `[x]`), ARCHITECTURE.md (Chat.tsx en el árbol) e index.html (tecla `c` + requisito de Ollama). Validado con tsc + 154 tests + smoke de render + e2e real con llama3.2 ("¿cuánto gasté en marzo?" → respuesta con totales por ítem).
- 2026-08-14: Creado `ollama-test.html`, un playground web que explica y permite probar la API local de Ollama (enlazado desde index.html: nav, hero y sección "Run It Live"). Incluye endpoints (GET `/`, `/api/version`, `/api/tags`; POST `/api/chat`), testers interactivos de estado y de chat (POST con body editable, muestra request/response), un demo de tool-calling con las 5 tools reales de `src/ai/tools.ts` (envía el array de tool definitions y muestra los `tool_calls`), referencia de campos del body/respuesta y troubleshooting. Verificado que Ollama responde CORS con `Access-Control-Allow-Origin` para orígenes HTTP pero **403 para `file://` (Origin: null)**, por eso la página advierte que debe servirse con `bunx serve .` o `python3 -m http.server`. Además creado el `.env` real (gitignored) con `OLLAMA_HOST` y `AI_MODEL`; verificado que `bunx serve .` sirve ambas páginas (200/301). Sin cambios de TS; tsc + 154 tests OK.
- 2026-08-14: Completado el paso 5 de v5 (Categorización por sugerencia al ingresar) en la branch `feat/022-expense-categorization-suggestion`. `src/utils/suggestItem.ts` (puro y testeable): `suggestItem(items, expenses, description)` con scoring determinístico por ítem (name: 1.0 si descripción == nombre, 0.8 si se contienen; history: 1.0 si la descripción aparece en un gasto previo del ítem, 0.9 por solapamiento de tokens) con umbral 0.8, más `parseSuggestionAnswer` que mapea la respuesta textual del LLM al ítem (case/accent-insensitive vía `normalize`, ahora exportado desde filters.ts). `src/ai/suggest.ts`: `suggestItemWithAgent` reutiliza `agent.ask` con un prompt acotado y propaga errores de conexión (Ollama caída) para mostrarlos en la UI. UI: `Form.tsx` extendido con `onValuesChange` (notifica cambios en vivo), `onAsk` (tecla `?` en campos de texto dispara la IA) y `setValue` (push programático de un valor a un campo con nonce, para preseleccionar el Ítem). `ExpenseForm.tsx` reordena campos (Descripción primero): sugerencia en vivo "🤖 AI sugiere: Recurrente — Alquiler" que preselecciona el Ítem sin pisar la elección manual del usuario (itemTouched), `?` pide la sugerencia al LLM (estado "pensando…", errores en rojo), y en edición no pisa el ítem ya elegido. `App.tsx` pasa `expenses` y `agent` al form. Decisiones: la sugerencia nunca guarda sola (solo preselecciona; el humano confirma), no se agregó tool nueva al registry (queda para el paso MCP). Sincronizados ROADMAP.md (categorización marcada `[x]`), ARCHITECTURE.md e index.html. Validado con tsc + 173 tests + smoke de render del form.
- 2026-08-14: Fix en el chat (v5 paso 4): al preguntar "¿Cuál es el año actual?" el modelo respondió mal ("2024") y tardó 37s. Causas medidas: llama3.2 se niega a responder fechas (insiste en su cutoff de entrenamiento 2023 aunque el system prompt diga lo contrario) y, al no saber la fecha, ejecutaba tools en iteraciones lentas de ~8s cada una. Fix en 2 partes: 1) `src/ai/agent.ts` ahora tiene `buildSystemPrompt(now?)` que inyecta la fecha actual en el system prompt (útil para preguntas relativas al mes/año), y `createAgent` lo usa como default; 2) nuevo `src/utils/dateIntent.ts` con `answerDateQuestion(question, now?)` (puro y testeable, case/accent-insensitive) que intercepta en `Chat.tsx` antes de llamar al LLM las preguntas de fecha/año/mes actual y responde desde el reloj de la app → correcto e instantáneo (~0ms vs 26-37s). Medido también: una pregunta de datos reales ("¿cuánto gasté en marzo?") tarda ~34s con llama3.2+tools (inherente al modelo+contexto grande, optimizable a futuro con streaming/contexto menor). Validado con tsc + 178 tests.
- 2026-08-24: Primera versión de creación de gastos por chat (v6 parcial) en la branch `feat/031-chat-create-expense`. Nuevo módulo puro `src/ai/expenseIntent.ts`: prompt extractor (`EXPENSE_INTENT_SYSTEM_PROMPT`) que devuelve JSON estricto (`{"intent":"create_expense", itemName/itemType/description/amount/installmentsTotal}` o `{"intent":"none"}`), `parseExpenseIntentResponse` defensivo (stripping de fences/prosa, amount como número o string es-AR vía parseCurrency, itemType validado contra ITEM_TYPES, cuotas default 1), `extractExpenseIntent(provider, question)` con una llamada directa sin tools, `findItemForConcept(items, concept)` (match exacto/substring/subset de tokens, case-accent-insensitive, gana el nombre más largo) y `toTitleCaseEs`. El registry de tools SIGUE solo lectura: la escritura va por callbacks. `Chat.tsx`: tras `answerDateQuestion`, intercepta con extracción; si hay intención muestra borrador ("🤖 Detecté un gasto para crear" con ítem existente o nuevo + tipo inferido, monto total y cuota calculada) y pide s/n; `s` crea ítem (si falta, título formateado, tipo inferido u `other`) + gasto (fecha hoy, ownership 100%, installments {total:n,current:1} si n>1); `n` cancela; Esc cancela pendiente en vez de volver; footer cambia a "s Confirmar · n Cancelar" mientras hay pendiente. `App.tsx`: provider hoisted a `useState` propio y handlers silenciosos `handleChatCreateItem` (devuelve el Item creado)/`handleChatCreateExpense` con refresh() sin navegación. ROADMAP.md: v6 pasa de "NO planificada" a implementación parcial documentada (pendientes: tool WRITE en registry, diálogo guiado de datos faltantes, ownership compartido). Smoke con mocks de stdin/stdout según protocolo real de ink: stdin debe implementar `readable`+`read()` (buffer interno), NO eventos `data`; stdout necesita `.on()`/EventEmitter y `isTTY=true`; script del smoke DEBE vivir dentro del repo para no duplicar la copia de React (si no: "invalid hook call"). Validado con tsc + 237 tests + smoke 5/5 (match existente, creación de ítem nuevo tipo other, cancelación). Merged como PR #32.
- 2026-08-24: Vigencia mensual al crear gastos en la branch `feat/032-fixed-months-expenses`. Caso de uso del usuario: "Alquiler $560.000 durante 4 meses" → que aparezca el mismo valor en agosto, septiembre, octubre y noviembre. Decisiones tomadas por preguntas (todas las opciones recomendadas): N registros físicos (no un campo expandible virtual), mismo día de mes con clamp (31 ene→28 feb), vigencia excluyente con cuotas, solo formulario (el chat queda para después). Nuevo helper puro `src/utils/fixedMonths.ts`: `expandFixedMonths(base, months)` (valida entero 1..MAX_FIXED_MONTHS=24, rechaza installments, clon descripción/monto/ownership, fechas consecutivas con clamp via Date.UTC days-in-month), `fixedMonthsPreview(dateIso, months)` ("Se crearán 4 registros: ago 2026 – nov 2026") y `MAX_FIXED_MONTHS`. `ExpenseForm.tsx`: prop `allowFixedMonths` (default false — solo alta, NUNCA en edición para no duplicar registros), campo "Vigencia (meses)" insertado condicionalmente tras Fecha con índices dinámicos (`idx` map en lugar de constantes numéricas), preview en vivo verde y validaciones (entero 1..24, "Usá cuotas o vigencia, no ambos"); `onSubmit` ahora emite `NewExpense[]`. Lección técnica: el preview NO se refrescaba porque `valuesRef` es un ref y no dispara re-render al tipear en Vigencia (los otros campos re-renderizaban de casualidad porque setSuggestion cambia); fix = espejo en estado `liveValues` actualizado en handleValuesChange. `App.tsx`: `handleAddExpense(inputs: NewExpense[])` itera createExpense + refresh + navega al itemDetail del primero; edición usa `inputs[0]`. El Dashboard mensual los muestra solos (agregación por fecha intacta); charts/tools/search sin cambios. Validado con tsc + 245 tests (+8: expansión, clamp, año bisiesto, cruce de año, preview, exclusión) + smoke 8/8 del formulario (4 registros con fechas correctas, error de exclusión sin submit, preview visible, modo edición sin campo). Merged como PR #33.
- 2026-08-24: Renombrada la terminología visible de "Ítem" a "Grupo" en la branch `feat/033-grupo-terminology`. Motivación del usuario: el modelo mental tiene grupos con varios gastos internos (Tarjeta Naranja → TV 6 cuotas, Amazon 12 meses, Cama 1 mes) Y conceptos sueltos sin sub-gastos visibles (Alquiler mismo valor x meses, Expensa variable); decir "ítem"/"gasto" confundía. Decisión clave: se renombra SOLO lo que ve el usuario (UI + prompts IA + docs); los nombres internos (`Item`, `itemId`, tabla `items`, tool `list_items`) quedan como detalle de implementación, documentado como nota de terminología en ARCHITECTURE.md. Barrido con sed de Ítems/ítems/Ítem/ítem → Grupos/grupos/Grupo/grupo en: Dashboard (footer "Agregar Grupo", empty states), App titles ("Agregar/Editar Grupo"), ExpenseForm (label "Grupo:", hint "No hay grupos creados", error "Selecciona un grupo", texto de sugerencia IA), Chat (borradores "Grupo existente/nuevo"), SearchPalette ("buscar en conceptos, grupos y personas", "Ir al grupo"), Charts ("Por tipo de grupo", "Top grupos del año"), ExpenseDetail ("Grupo:"), prompts (DOMAIN_SYSTEM_PROMPT con "Grupos: contenedores de gastos...", descripciones de las 7 tools, extractor expenseIntent, prompt de suggest.ts) y fixtures de tests que simulaban salida del LLM (agent.test.ts, suggestItem.test.ts — parseSuggestionAnswer no depende de la palabra). Docs: ROADMAP.md con conceptos actualizados (identificadores de código intactos), index.html en inglés (group/Add group/Edit group). Smoke 5/5 renderizando Dashboard+ExpenseForm reales (footer nuevo, label Grupo, Vigencia intacta, hint sin grupos). Validado con tsc + 245 tests. Próximo paso acordado (Fase 2): `feat/034-auto-group-expenses` — grupo automático al cargar gasto: campo Grupo opcional con pseudo-opción "— crear grupo nuevo —", matching con findItemForConcept/toTitleCaseEs existentes, helper puro inferGroupType(description) por palabras clave (tarjeta→credit_card, auto→car, alquiler/expensa/depto→home, nenas/colegio→kids, resto other), preview "🤖 Se creará el grupo 'X'", resolución en handleAddExpense; edición nunca auto-crea.
- 2026-08-24: Fix de footer del Dashboard incluido en feat/033 (decisión del usuario de no abrir branch aparte). Bug reportado tras borrar el `.data/expenses.db`: con DB vacía desaparecían del footer `a Agregar Gasto · e Editar · d Eliminar` (condición `items.length > 0`). Era inconsistente: la tecla `a` siempre funcionó sin grupos (abre ExpenseForm que guía con "No hay grupos creados"), estaba escondida justo cuando más se necesita; y `e/d` se mostraban con grupos creados aunque el mes no tuviera registros (inertes por guard `rows.length === 0`). Fix en Dashboard.tsx: `a Agregar Gasto` SIEMPRE visible; `e Editar · d Eliminar` visibles solo cuando `rows.length > 0` (grupos con gasto en el mes actual — misma condición que habilita navegación/selección). Smoke 5/5 de los 3 estados (DB vacía muestra a sin e/d; grupos sin registros del mes ídem; con registros muestra a/e/d). Validado con tsc + 245 tests.
- 2026-08-24: Grupo automático al crear gastos en la branch `feat/034-auto-group-expenses` (Fase 2 del redesign acordado tras el renombre a "Grupo"). Nuevo helper puro `src/utils/autoGroup.ts`: `inferGroupType(description)` (keywords case/accent-insensitive en orden de prioridad: tarjeta/visa/mastercard/amex/naranja→credit_card, auto/coche/nafta/patente→car, alquiler/expensa/depto/casa/mudanza→home, nena(s)/colegio/facultad/alimentaria→kids, resto other) y `resolveAutoGroup(description, items)` (reutiliza findItemForConcept + toTitleCaseEs de expenseIntent.ts; devuelve {matched?, newName, newType}). `ExpenseForm.tsx`: prop `allowAutoGroup` (solo alta desde Dashboard) que agrega la pseudo-opción `— crear grupo nuevo —` (value '') al select, cambia `initialItemValue` para NO caer al primer ítem cuando initial es '', y muestra preview "🤖 Se creará el grupo 'X' (tipo)" o "🤖 Grupo: existente" cuando el campo quedó en '' y hay descripción; submit permite itemId '' si hay descripción. `App.tsx`: `handleAddExpense(inputs)` resuelve UNA vez el grupo (matched ?? createItem con nombre title-cased y tipo inferido) y le engancha todos los registros (incluye vigencia N); preselección desde ItemDetail intacta. Lección del smoke: "zapatillas naranja" NO matchea "Tarjeta Naranja" porque findItemForConcept exige subconjunto completo de tokens (1 token compartido no alcanza, anti-falsos-positivos); el matcher conservador es correcto. Validado con tsc + 250 tests (+5 inferencia/resolución) + smoke 7/7 key-by-key (concepto nuevo emite '' y preview con tipo inferido home, match por tokens muestra grupo existente, preselección sin preview, vigencia+auto-grupo = 4 registros ''). Merged como PR #35.
- 2026-08-24: Campo Grupo híbrido + fin del alta de grupo independiente en la branch `feat/035-hybrid-group-field`. Pedido del usuario: desde el Dashboard solo "Agregar Gasto", y en el alta escribir el grupo directamente (ej: "Zapatillas rojas" en grupo "Tarjeta de Credito Naranja" tipeado, que se crea solo). Decisiones por preguntas: cerrar feat/034 primero (PR #35), ELIMINAR la pantalla de alta de grupo (los grupos nacen al cargar gastos; editar sigue desde el detalle) y matchear lo escrito contra existentes antes de crear. Implementación: `Form.tsx` suma tipo `combo` (texto libre + ←→ cicla opciones; tipear sobre un preset REEMPLAZA con texto custom; primer backspace sobre preset limpia a ''; cursor oculto mientras se muestra un preset); `autoGroup.ts` agrega `resolveGroupForSave(rawGroupId, description, items)` puro con prioridad id explícito → texto tipeado (match o nombre NUEVO tal cual lo tipeó, sin Title Case) → fallback descripción (Title Case, comportamiento feat/034); `ExpenseForm` usa combo solo con `allowAutoGroup` (edición mantiene select clásico) y el preview ahora distingue "Se usará el grupo existente X" vs "Se creará el grupo Y"; `App.handleAddExpense` resuelve UNA vez con resolveGroupForSave y engancha todos los registros (vigencia N incluida); eliminados screen `addItem`, caso en getBackScreen, handler y prop `onAddItem` (types/index.ts sin la variante). Fix cosmético de raíz: el espacio tras ":" en los labels del Form se perdía por trimming de whitespace JSX (`{field.label}`+`{": "}`). Lecciones del smoke: ink escribe DIFFS parciales (asertar sobre el buffer acumulado da falsos negativos de posición — validar comportamiento por submit); disparar teclas SIN sleep entre chars pierde updates por closure obsoleto de values en useInput (12ms entre teclas es obligatorio); los scripts probe DEBEN vivir dentro del repo (copia duplicada de React). Validado con tsc + 255 tests (+5 resolveGroupForSave) + smoke 10/10 (grupo nuevo tipeado tal cual con tipo inferido crédito, match "tarjeta naranja"→existente, ←→ preset + tipeo encima = nombre custom, edición select clásico, footer sin i y empty state apuntando a 'a').
- 2026-08-25: Vista de detalle unificada en la branch `feat/036-unified-detail-grid`. Motivación: el usuario reportó que "T. Naranja" (tipo `credit_card`) se veía diferente a "Depto-Gus" — la grilla mes a mes no se mostraba. Decisión: eliminar `ItemDetailCard.tsx` por completo y unificar TODOS los grupos bajo `ItemDetail` con la grilla mensual. Cambio en `App.tsx`: eliminada la condición `item.type === "credit_card"` que bifurcaba el routing. Fix de proceso: los cambios se hicieron accidentalmente sobre `main` (violación de la regla del harness); se corrigió creando la branch `feat/036` desde `main` y moviendo los cambios (committed post-fix). Smoke end-to-end con DB temporal y grupo `credit_card`: verificó que se renderiza la grilla con encabezados Ene–Dic y el gasto "Supermercado", y que la vista de tarjeta individual ya no existe. Validado con tsc + 264 tests + smoke 4/4. Merged como PR #37.
- 2026-08-24 (mismo día, dentro de feat/035): Consolidación de vigencia en el detalle de grupo. El usuario creó "Alquiler" con Vigencia=3 y grupo "Depto-Gus": el Dashboard lo agregaba bien pero ItemDetail listaba 3 filas crudas "Alquiler" (los N registros físicos de feat/032 sin consolidar). Confirmado por preguntas que usó Vigencia=3 (no era duplicación). Nuevo helper puro `src/utils/detailRows.ts`: `buildDetailRows(expenses, year)` particiona gastos del año — registros SIN cuotas se agrupan por clave (descripción + monto + ownership) en UNA fila con `coveredMonths` unión y `expenseIds` ordenado por fecha ([0] = target de Enter); registros CON cuotas siempre fila propia (badge "en N ctas", primera cuota verde); `findRowContaining(rows, expenseId)` para el focus desde búsqueda. `ItemDetail.tsx` refactorizado a las filas consolidadas. Regla conservadora: distinta descripción/monto/ownership NO mergea. ItemDetailCard intacto (vista por gasto individual, no grilla). Smoke 4/4 (una fila Alquiler, $560.000 en 3 celdas, TV con cuotas separada, $100.000 de cuota). Lecciones: la grilla mide ~210 cols — el mock de stdout necesita columns≥260 o ink trunca celdas; un warning React de keys es PREEXISTENTE en HEAD (aparece solo con filas múltiples, no reproducible aislado, cosmético — no introducido por este cambio). Validado con tsc + 264 tests (+9 detailRows).
- 2026-08-17: Backlog de v5 completado (PRs #23–#27), previamente no documentado en memory.md. PR #23 `chore/023-remove-seed-data-and-improve-empty-state`. Detección de patrones (PR #24/#26, branch `feat/024-expense-pattern-detection`): `src/utils/patterns.ts` puro con `calcMonthChanges` (delta y %), `calcMovingAverage`, `calcTrend` (regresión lineal, dirección up/down/stable con umbral 5% del promedio), `findAnomalies` (z-score > 2 sobre valores > 0), `detectRecurringItems` (≥3 meses activos y estabilidad ≥ 0.7) y `buildPatternData`; expuesto en el registry como tool `analyze_patterns` (`src/ai/tools.ts`). Recomendaciones (PR #25, branch `feat/025-recommendations`): `src/utils/recommendations.ts` con `generateRecommendations` (reglas determinísticas: `spending_increase` >15%, `spending_decrease` <-15%, `category_spike` >2x promedio con ≥4 meses, `new_recurring`, `top_cost_driver` >40% del total; ordenadas por severidad high>medium>low) expuestas como tool `get_recommendations`. MCP server (PR #27, branch `feat/026-mcp-server`): `src/mcp/server.ts` con `@modelcontextprotocol/sdk`, expone TODAS las tools de `buildTools` con `readOnlyHint` por tool, arranca por stdio con `DB_PATH` (default `expense-tracker.db`) y se registró en `opencode.json`.
- 2026-08-22: PR #28 `refactor/027-update-mcp-server-config`: `opencode.json` pasa el MCP a `type: "local"` con `environment.DB_PATH`.
- 2026-08-24: PR #29 `bug/028-fix-add-expense-empty-concept` (fix del alta de gasto con concepto vacío); PR #30 `feat/029-monthly-dashboard` (Dashboard pasa de tracking por año a por mes + `parseCurrency` en `src/utils/format.ts`); PR #31 `feat/030-item-types-v2`: reemplaza los tipos de ítem por categorías de dominio (`credit_card`, `kids`, `car`, `home`, `other`) con migración de valores legacy.
- 2026-08-25/26: PRs #37–#45 (post-feat/036). PR #38 `docs/037-assign-prs-to-user` (regla: asignar PRs a `slaveofthecode` con `gh pr edit <num> --add-assignee`; corregida la sintaxis en PR #39). PR #39 `docs/038-sync-docs-with-app`: sincronización total de documentación (README, index.html) con el estado real, incluyendo las tools de v5 y el MCP. PR #40 `feat/039-fix-chat-search-prompt`: el system prompt del agente fuerza el uso de tools en preguntas factuales (evita que el modelo invente datos). PR #41 `feat/040-chat-intent-items-context`: los grupos existentes se pasan al extractor de intención para un matching preciso. PR #42/#43 `feat/041-chat-group-matching` (re-merge del mismo branch): mejorado el matching de grupos al crear por chat con `resolveGroupName`. PR #44 `fix/042-chat-credit-card-group-matching` y PR #45 `fix/043-cc-heuristic-override`: extracción de referencia de tarjeta de crédito (`extractCreditCardRef`) y heurística de override para que priorice la tarjeta que el usuario nombra ("con la naranja", "tarjeta T. Cordobesa") sobre el nombre del producto. State final en main: 281 tests / 17 files.
- 2026-08-30: Sync de docs al detectar drift (`docs/044-sync-docs-real-state`). Hallazgos: memory.md y ROADMAP.md quedaron desincronizados desde el 17/08 — v5 (patrones, recomendaciones, MCP) ya estaba implementado en main desde PRs #24–#27 pero ROADMAP lo marcaba `[ ]`; memory.md no registraba PRs #23–#31 ni #37–#45. Acciones: ROADMAP.md marca v5 completo `[x]` con descripción de `patterns.ts`/`recommendations.ts`/`mcp/server.ts`; v6 documenta las mejoras de matching por chat (PRs #40–#45); memory.md recibe el backfill de sesiones y la corrección de "PR #37 pendiente" → "Merged como PR #37". Verificado con `bunx tsc --noEmit` + 281 tests. Pendientes reales de aquí en adelante (v6): tool WRITE `create_expense` en el registry, diálogo guiado de datos faltantes y ownership compartido desde el chat.
- 2026-08-30: Completados los 3 pendientes de v6 (la branch docs/044 se cierra como PR de docs y las partes de código salen en branches/frente propias). 1) **Tool WRITE `create_expense`**: `tools.ts` pasa de `readonly: true` literal a clasificación por tool; `buildTools(ToolsContext)` incluye `create_expense` solo con contexto de escritura (`WriteToolsContext` con `createItem`/`createExpense`); nueva `createChatTools(db)` (lectura + escritura) para el chat mientras `createReadTools(db)` se mantiene solo lectura para el MCP. Parámetros: `itemName` (matchea grupo existente vía `findItemForConcept` o lo crea con `toTitleCaseEs` + tipo inferido por `inferGroupType`), `description`, `amount`, `itemType`, `installmentsTotal`, `date`, `ownershipPercentage`, `ownershipPerson`; helpers `coerceAmount`/`asPositiveInt`/`asDate`/`clampPercentage`. `agent.ts` suma `onWriteCall` (confirmación humana obligatoria: sin handler la tool de escritura devuelve error "requiere confirmación humana" y no escribe). 2) **Diálogo guiado**: `expenseIntent.ts` agrega `create_expense_incomplete` (el extractor lista `missingFields` en vez de devolver undefined), `parseAmountFromText` (regex de montos, gana el número mayor), `missingFieldsOf`, `applyGuidedAnswer` (rellena un campo por turno, re-parsea amount inline); `Chat.tsx` suma estado `collecting` con prompts por campo (`FIELD_PROMPTS`), Enter responde y Esc cancela. 3) **Ownership desde el chat**: prompt del extractor entiende "a medias", "mitad y mitad", "compartido con X" → `ownershipPercentage`/`ownershipPerson` (persona sin reparto normaliza a 50%; sin datos no se emite la clave para no romper `toEqual` de tests); el borrador muestra "Compartido: 50% con Gus" y `confirmPending` aplica ownership al crear (porcentaje default 100). MCP queda oficialmente solo lectura (decisión final: la escritura con confirmación es exclusiva del chat). ROADMAP v6 pasa los 3 ítems a implementado con decisiones finales. Validado con `bunx tsc --noEmit` + 298 tests (0 fail) + build OK.
- 2026-08-30: Config MCP apuntando a la DB real en la branch `chore/047-mcp-db-path-sync`. Hallazgo detectado al armar el testeo: `opencode.json` registraba el MCP con `DB_PATH=expense-tracker.db` (archivo inexistente) mientras la app usa `.data/expenses.db` → el MCP devolvía datos vacíos. Acciones: fijado `DB_PATH=.data/expenses.db` en `opencode.json`; versionada la registración nativa por herramienta (`.mcp.json` en raíz para Claude Code y `.cursor/mcp.json` para Cursor; Copilot y Claude Desktop quedan como config manual documentada en README); `.env.example` documenta `DB_PATH` y `AI_MODEL=phi4-mini`. `.env` local pasa a `AI_MODEL=phi4-mini` (único modelo descargado; `llama3.2` ya no está localmente). Decisión del usuario: NO se toca el default `expense-tracker.db` de `src/mcp/server.ts:66` — el path viaja por env/`.env` por cliente, no en código.
- 2026-08-30: Fix del runtime del MCP server en la branch `chore/048-mcp-server-bun-runtime`. Detección del testeo (probe con SDK client vía stdio): `bunx tsx src/mcp/server.ts` ejecuta tsx bajo **Node**, que no entiende el scheme `bun:` → el server moría al importar `bun:sqlite` y el MCP nunca conectaba. Fix: todas las registraciones pasan a `bun src/mcp/server.ts` (bun ejecuta TS nativo y provee `bun:sqlite`): `opencode.json`, `.mcp.json` (Claude Code) y `.cursor/mcp.json`; README actualizado con la advertencia de no usar `bunx tsx`. Verificado con `scripts/mcp-probe.ts` (probe del SDK client que lanza el server por stdio con `DB_PATH=.data/expenses.db`): 7 tools presentes, todas `readOnlyHint=true`, sin `create_expense`; llamadas reales a `list_items`/`list_expenses`/`get_monthly_summary`(2027-07)/`get_yearly_summary`(2027)/`search_expenses`("Rentas")/`analyze_patterns`/`get_recommendations` con la DB real; errores descriptivos (`isError`) en mes con formato inválido, argumento requerido faltante y `create_expense` inexistente. State final: 298 tests.
- 2026-08-30: Sesión de testeo integral (MCP + Ollama local + offline). Notas de sesión en `docs/049-testing-session-notes`. Resultados: 1) **MCP** verificado con `scripts/mcp-probe.ts` (SDK client real vía stdio + DB real): 7 tools, todas `readOnlyHint=true`, sin `create_expense`, datos reales y errores descriptivos — ALL PASS. Para probarlo desde opencode hay que recargar la sesión (el server arrancó con la config vieja). 2) **Modelos locales**: `phi4-mini` NO soporta native tool-calling en Ollama (devuelve el JSON de la tool como texto, `toolCalls:0`) pero es EXCELENTE extrayendo intención de alta (tipo inferido, monto, ownership "a medias con Gus"→50%+persona); `llama3.2` SÍ hace tool-calling (Q&A de datos funciona, aunque la interpretación es flaky: un intento dijo "$0" y el siguiente acertó "$61.200") pero es débil extrayendo intención (devuelve `none` o repite ejemplos del prompt, inventa montos en el diálogo guiado). **Decisión del usuario: single-model `AI_MODEL=llama3.2`** (se descarta dual-model). Limitación documentada: con modelos chicos el chat Q&A y el diálogo guiado tienen calidad variable; la app usa UN modelo para ambas rutas. 3) **Offline/errores**: `OLLAMA_HOST` a puerto cerrado → `OllamaConnectionError` propagado por `extractExpenseIntent` y `agent.ask` (UI lo muestra en rojo) y health-check `ok:false` con hint; modelo inexistente → "is running, but model X is not pulled. Run: ollama pull X". `.env` final: `AI_MODEL=llama3.2`, `DB_PATH=.data/expenses.db`. Regresión: tsc + 298 tests + build OK.
- 2026-08-30: Limpieza de housekeeping (branch `chore/051-cleanup-model-env-and-stale-branches`): 1) `.env.example` pasó de `AI_MODEL=phi4-mini` a `AI_MODEL=llama3.2` (estaba desincronizado con la decisión single-model de la sesión de testeo); 2) borrados los 47 branches remotos stale (todos merged) de `origin` — verificados con `git merge-base --is-ancestor <branch> main` antes de cada `git push origin --delete`. State final: 303 tests.
- 2026-08-30: Streaming en el chat (v6, branch `feat/050-chat-streaming`). Las respuestas de Q&A se muestran token a token en vivo en vez del "pensando…" fijo. Cambios: `LLMProvider.chat(messages, tools, options?)` acepta `ChatOptions.onToken` → envía `stream: true` y consume el NDJSON de `/api/chat` línea a línea (`readStream`); los `tool_calls` que llegan fraccionados se fusionan por índice (`mergeStreamedToolCalls`) conservando el `id` de Ollama; `parseToolCalls` ahora prefiere el id real sobre `call_N`. `Agent.ask(question, { onToken })` propaga el callback por todas las iteraciones (param renombrado a `askOptions` para no shadowear el `options` del closure). `Chat.tsx` muestra burbuja en vivo "🤖 <tokens>▌" mientras streamea (verificado e2e real: 15 tokens idénticos a la respuesta final, tras ~50s de tool-calling). Aprendizaje de http: Ollama manda `tool_calls` completos (con id y arguments como objeto) típicamente en UN chunk antes de `done`; content llega como deltas por línea. tsc + 303 tests (+5: 4 streaming provider + 1 onToken agent) + build OK. Sync: ROADMAP v6, ARCHITECTURE árbol, index.html ("answers stream in live"). Pendiente humano: validar la burbuja visualmente con `bun start` (la flakiness de respuesta de llama3.2 a veces dice "no hay registros" aunque haya datos — limitación conocida del modelo).
